@model IEnumerable<University.Models.ModelViewTKSV>
@{
    ViewBag.Title = "TKSinhVien";
    Layout = "~/Views/HomePage/Admin.cshtml";
}


<table class="table">
    <tr>
        <th>
           Danh Sách Sinh Viên
        </th>
      
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.tensv, "CTTK", new { id = item.masv })
            </td>
            <td>
                @Html.ActionLink("Thêm", "CreateNewStudent") 
            
            </td>
        </tr>
    }

</table>